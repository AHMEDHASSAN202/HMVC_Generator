<?php

namespace App\Modules\{{module_name}}\Http\Middleware;

use Closure;

class {{middleware_name}}
{

    public function handle($request, Closure $next, $guard = null)
    {
        return $next($request);
    }
}
